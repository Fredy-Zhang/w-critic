# w-critic
WGAN-GP based W-Critic for estimating Wasserstein Distance between real and generated image distributions. Used as an evaluation metric to compare generative models. Supports training and evaluation across multiple model outputs.
